<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.codestepfish.datasource.mapper.AdminMapper">
    <select id="listAdmins" resultType="com.codestepfish.datasource.entity.Admin">
        select a.id id,
        t.id tenant_id,
        t.tenant_name tenant_name,
        a.account account,
        a.username username,
        a.phone phone,
        a.status status,
        a.role_id role_id,
        r.role_name role_name,
        a.create_time create_time,
        a.update_time update_time,
        a.delete_time delete_time
        from admin a
        left join tenant t on a.tenant_code = t.code
        left join role r on r.id = a.role_id
        where a.delete_time is null
        and t.delete_time is null
        <if test="account != null and account != ''">
            and a.account = #{account}
        </if>
        <if test="phone != null and phone != ''">
            and a.phone = #{phone}
        </if>
        <if test="tenant_code != null and tenant_code != ''">
            and a.tenant_code = #{tenant_code}
        </if>
        order by a.create_time asc
    </select>
</mapper>