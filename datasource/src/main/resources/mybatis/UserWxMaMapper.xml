<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.codestepfish.datasource.mapper.UserWxMaMapper">
    <select id="findByTenantIdAndOpenId" resultType="com.codestepfish.datasource.entity.User">
        select u.*
        from user u,
             user_wx_ma uwa
        where u.id = uwa.user_id
          and u.tenant_id = #{tenant_id}
          and uwa.open_config_id = #{open_config_id}
          and uwa.openid = #{openid}
          and u.delete_time is null
          and uwa.delete_time is null
    </select>
</mapper>